[Unit]
Description=AILogAnalyzer - AI agent log analysis (%i)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/AILogAnalyzer
ExecStart=/usr/bin/docker exec -T ailog-agent-%i /tools/analyze.sh
TimeoutStartSec=600
